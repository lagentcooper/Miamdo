// Numéro de version de l'app — source unique.
// À incrémenter ici, puis reporter dans `sw.js` (VERSION) pour que le cache
// hors ligne soit renouvelé, et dans CHANGELOG.md.
export const APP_VERSION = '1.6.1';
